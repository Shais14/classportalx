<% provide(:title, 'All students') %>
<h1>All students</h1>

<ul class="students">
  <% @students.each do |student| %>
    <li>
      <%= link_to student.name, student %>
      <%= student.email %>
    </li>
  <% end %>
</ul>